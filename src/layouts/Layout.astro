<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Blizzard Enterteintment</title>
		<script is:inline>
			try {
				const savedTheme = localStorage.getItem('theme') || 'w';
				document.documentElement.setAttribute('data-theme', savedTheme);
			} catch {}
			try {
				const savedLang = localStorage.getItem('lang') || 'es';
				document.documentElement.setAttribute('data-lang', savedLang);
				document.documentElement.lang = savedLang;
			} catch {}
		</script>
	</head>
	<body>
		<slot />
		
	</body>
</html>

<style>
    :root {
        --accent: #0074e0;
        --accent-weak: #0074e0aa;
        --text-color: #ffffff;
        --page-bg-color: white;
        --border-color: #ccc;
        --bg-image: url('/backg.webp');
    }

    html[data-theme="w"] {
        --accent: #0074e0;
        --accent-weak: #0074e0aa;
        --text-color: #ffffff;
        --page-bg-color: white;
        --border-color: #ccc;
        --bg-image: url('/backg.webp');
    }

    html[data-theme="d"] {
        --accent: #d7263d;
        --accent-weak: rgba(215, 38, 61, 0.35);
        --text-color: #ffffff;
        --page-bg-color: white;
        --border-color: #b33;
        --bg-image: url('/backd.webp');
    }

    html[data-theme="h"] {
        --accent: #c7882f;
        --accent-weak: rgba(199, 136, 47, 0.35);
        --text-color: #ffffff;
        --page-bg-color: white;
        --border-color: #6aa8d9;
        --bg-image: url('/backh.webp');
    }

    html[data-theme="h"] body {
        background-size: cover;
        background-position: top center;
        background-repeat: no-repeat;
        background-origin: padding-box;
        background-clip: padding-box;
    }

    body {
        margin: auto;
        padding: 1rem;
        width: 100%;
        max-width: 400px;
        height: 95vh;
        font-family: Arial, Helvetica, sans-serif;
        border: 1px solid var(--border-color);
        background-image: var(--bg-image);
        background-repeat: no-repeat;
        background-position: top;
        background-size: cover;
        color: var(--text-color);
        transition: background-color 0.5s ease, color 0.5s ease, border-color 0.5s ease, background-position 0.5s ease, background-size 0.5s ease, background-image 0.5s ease;
    }

    html { background-color: var(--page-bg-color); }
</style>
